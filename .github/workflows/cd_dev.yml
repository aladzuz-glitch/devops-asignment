name: CD DEV

on:
  push:
    branches:
      - main

jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

      - name: Deploy
        run: |
          echo "Deploying application to DEV environment..."